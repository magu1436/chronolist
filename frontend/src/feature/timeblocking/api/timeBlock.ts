import type { TimeBlockSource } from "../types/blockSourceTypes";


/**
 * タイムブロックの情報を更新するAPI.
 * 
 * @param block 更新後のブロックの情報
 */
export const update = (block: TimeBlockSource): void => {
    return;
}